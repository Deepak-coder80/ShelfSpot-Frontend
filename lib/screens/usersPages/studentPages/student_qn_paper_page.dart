import 'package:flutter/material.dart';

class StudentQnPaperPage extends StatelessWidget {
  const StudentQnPaperPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor:  Color(0xFF201F15),
      body: Center(
          child: Text('qn Paper Page')
      ),
    );
  }
}
